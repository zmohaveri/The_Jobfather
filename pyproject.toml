[tool.poetry]
name = "the-jobfather"
version = "0.1.0"
description = ""
authors = ["zmohaveri"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.13"
langchain = "^1.3.1"
pydantic = "^2.13.4"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
